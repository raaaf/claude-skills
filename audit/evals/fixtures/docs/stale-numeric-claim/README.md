# Account Catalogue

The full chart of accounts lives in `accounts.json`. Pickers only ever show the
curated subset, never the full catalogue.

## Curated subset

`accounts.json` marks the curated accounts with `"curated": true`. Of the 12
accounts in the catalogue, 8 are curated for the picker dropdown.
