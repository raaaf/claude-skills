{
  "fixture": "docs/agent-generated-em-dash/",
  "must_find": [
    {
      "dimension": "docs",
      "line": 8,
      "matches": ["em-dash", "—", "CLAUDE.md", "prose", "dash"],
      "severity": "minor"
    }
  ],
  "must_not_find": [
    {
      "dimension": "architecture",
      "reason": "doubling the queue-worker budget is a plain, documented design choice; the only defect is the punctuation in the docblock, not the logic"
    }
  ],
  "notes": "Regression fixture for freshly-written prose violating the project's own CLAUDE.md rules, distinct from the case a normal docs_sync check covers (a claim drifting from reality). Here the docblock is internally accurate, it just uses an em-dash the project's own CLAUDE.md explicitly forbids. Models the 2026-08-18 incident where fix-wave docblocks generated by the orchestrator itself used em-dashes twice, in violation of the exact rule the diff was being audited against (see agents/fix-agent.md, Completion self-checks item 5). No deterministic check covers this yet; it depends on a worker (or the self-check on generated text) reading the docblock against the project's own prose rules."
}
