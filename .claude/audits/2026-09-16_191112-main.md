# Audit 2026-09-16 19:11 — main (tenth run, gate attempt after run-9 fixes)

## Result

2 Critical, 26 confirmed findings, 0 unverified. Every in-scope dimension `complete`
(`typography`, `ui_design`, `animation`, `copy` skipped, no relevant files). Push marker NOT set by
rule: two Criticals open, both in `audit/SKILL.md`, both fixed in the follow-up commit.

## Scope

- Base HEAD `531f03c` (origin/main), head at start `8fb5ceb`, 26 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- find.js runId: `wf_169118cb-a96` (54 agents, 0 errors, 627s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`,
  `FRESH_SHELL_RESULT=OK`, eval validator 0 errors

## Findings

### Fixed in the follow-up commit

- [Critical][security] audit/SKILL.md:56: `pre-checks.sh` ran and nothing read its result; a `SECRET_SCAN_RESULT=FINDINGS` neither became a finding nor blocked the marker, although a secret in the diff is a Critical by this repo's own rule. Output is captured and printed, every `SECRET file:line: type` line is a `[Critical][security]` finding, and the Phase 4 conditions name it. `full-audit` captures it the same way (no marker there, but never silent).
- [Critical][architecture] audit/SKILL.md:107 and :250: `AUDIT_DIMENSIONS` gained `payments` inside the Phase 1.5 block and was tested in the Phase 4 block, a fresh shell where it does not exist, so `payments_head` was never recorded. Phase 1.5 prints the value; Phase 4 carries it as an explicit placeholder the orchestrator fills. The variable form of the fresh-shell class, which `check-fresh-shell.sh` does not see.
- [Important][docs_sync] CLAUDE.md:53: the lib row said "exactly one inline loop per SKILL.md", contradicting the per-block rule two paragraphs down, and listed half the functions. Rewritten with the rule and the full inventory; the lib header inventory gains the three helpers it omitted.
- [Important][code_quality] audit/bin/lib-orchestrator.sh:77: `orch_progress_claim` and `_touch` were byte-identical bodies presented as distinct. Claim is an alias of touch and says so.
- [Important][architecture] audit/bin/lib-orchestrator.sh:61: the resolver still tried a `CLAUDE_PROJECT_DIR` candidate the header calls hooks-only. Removed.
- [Important][architecture] design-audit/SKILL.md:64: the run-ledger start bypassed `orch_run_log`. Uses it.
- [Important][architecture] design-audit/SKILL.md:186: the worker briefing reused the "Common header" whose `MATCHED_GUIDELINES` is a diff match that does not exist in a repo-scope visual audit. Briefed as the fixed visual guideline set with `GUIDELINES_DIR`.
- [Important][security] design-audit/SKILL.md:56: `AUDIT_TMP` at a predictable path was `mkdir -p`'d without a symlink check while later phases Write finding data into it. A pre-existing symlink is refused.
- [Important][security] design-audit/agents/reference-verdict.md:37 vs SKILL.md:219: the consolidation rule assumed a `file:line` the Mobbin-sourced note cannot have. Location is the note's `file:line` when present, else the source it names.
- [Important][security] ship/SKILL.md:379: the host filter split on `:` and truncated bracketed IPv6; userinfo could also pose as the host. Parser drops userinfo, keeps `[...]` whole, denies loopback/link-local/ULA/mapped ranges; five-case test in the commit.
- [Important][architecture] find.js:211: mirrored `FRONTEND_EXT_RE` carried `/i` while every bash consumer is case-sensitive. Parity.
- [Minor][security] audit/bin/validate-locations.sh:22: line `0` passed the digit check. Rejected.
- [Minor][code_quality] ship/SKILL.md:49: `TEST_COMMAND` computed in Phase 0 and consumed nowhere (Phase 2b re-derives). Removed.
- [Minor][docs_sync] CLAUDE.md:106, audit/agents/w3-frontend.md:7, full-audit/SKILL.md:165: a Phase 0.5 that does not exist, a `maxTurns` comment citing a budget `prompt-template.md` never had, the finding-line contract pasted a second time. Each corrected or referenced.
- [Minor][docs_sync] five audit logs: the same open decision repeated per log. Recorded once in `learning-log.md` as a decision item; future logs reference it.

### Logged, not fixed

- [Important][security] audit/bin/lib-orchestrator.sh:53: marker paths predictable from `md5(cwd)`. Eighth run naming it; single-user `/tmp` convention, unchanged.
- [Minor][code_quality] audit/bin/lib-git-base.sh:23: two copy-paste loops in `resolve_default_branch`. Pre-existing, harmless.
- [Minor][performance] find.js:360, validate-locations.sh:26: `computeFloorFiles` twice per dimension, `wc -l` per pair. Small; left.

## Open Points

- The `UNCERTAIN`-blocks-marker decision: see `learning-log.md`, "Open decision".
- Variables crossing Bash blocks are not mechanically checked; the Critical above was found by a specialist.
