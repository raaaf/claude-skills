# Audit 2026-09-15 17:25 — main (second run)

## Result

0 Critical, 13 confirmed findings. Push marker NOT set: `docs_sync`, `security` and `architecture`
returned `incomplete`. The `skipped` half of the old blocker is gone (fixed in `311f9db`), so
`ui_design` no longer blocks; the remaining blocker is genuine coverage shortfall, not a rule defect.

## Scope

- Base HEAD `3475378`, head at start `311f9db`, 22 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- runId `wf_6b7f9158-d6c` (48 agents, 0 errors, 471s)
- Mechanical checks: all CLEAN or SKIP, `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`

## Findings

### Fixed in `29ae56e` (all five are defects in code this branch introduced)

- [Important][code_quality] audit/bin/check-silencing.sh:217: the awk pipeline ended in `|| true` under `set -euo pipefail`, so an awk runtime error was discarded and the script reported OK.
- [Minor][code_quality] audit/bin/check-silencing.sh:203: `key` computed and stripped but never read.
- [Important][code_quality] audit/bin/capture-screens.sh:64: the .gitignore block was copied from cache-write.sh without its symlink guard.
- [Minor][security] audit/bin/capture-screens.sh:146: predictable `$TMPDIR/...$$` fallback profile directory, removed by an EXIT trap.
- [Important][docs_sync] audit/references/audit-log-template.md:80: prose said 14 canonical ids above a list of 13; `payments` missing from the list a reader checks against.

### Logged, not fixed (pre-existing, outside what this branch touched)

- [Important][architecture] audit/agents/learning-agent.md:28: instructs the orchestrator to APPEND a trends block while line 171 of the same file says `TRENDS_BLOCK` REPLACES it.
- [Important][architecture] full-audit/SKILL.md:41: opens the ledger with `--skill full-audit` but defers Phase 4 to audit/SKILL.md, whose literal call writes a different skill name.
- [Important][architecture] delegate/SKILL.md:132: the `EXEC_REF` resolution loop still tries two candidate paths where the run-log loops try three (the sibling `CAPTURE` loop was fixed in `08d118e`).
- [Important][architecture] delegate/SKILL.md:56: the new test-lock claim does not hold for the executor, which is a separate agent definition (`agents/spec-executor.md`) with its own briefing.
- [Important][architecture] audit/references/learning-phase.md:7: plan-it's learning subsystem is an independently built parallel copy of audit's.
- [Important][security] CLAUDE.md:28: the invariant "subagents cannot write under `.claude/`" is restated five times as an absolute boundary, and the testing evidence behind it is thinner than the wording.
- [Important][security] audit/evals/run-evals.sh:1121: the nested eval session runs with the OS sandbox disabled while reading fixtures that deliberately contain hostile content.
- [Important][docs_sync] CLAUDE.md:64: `"Keine Findings."` is listed as a live contract identifier; it appears in no active agent or skill file, the same dead-identifier pattern corrected for two others in `08d118e`.
- [Important][docs_sync] plan-it/SKILL.md:143: the mandatory framing check is justified by "Evidence: 4 of 8 plans", the exact citation that `plan-it/SKILL.md:51` documents as untraceable; `:173` repeats the pattern.
- [Minor][docs_sync] plan-it/references/interview-guide.md:20: seven `Evidence:` citations anchor mandatory rules on plan logs that are gitignored and therefore uncheckable.
- [Minor][docs_sync] CLAUDE.md:90: the Effort section states plan-it's default but never /audit's (`medium`).
- [Minor][performance] audit/evals/run-evals.sh:444 and :754: `validate_expected()` and `score_against_log()` spawn four to five `jq` processes per expectation entry.

## Open Points

**The marker is still blocked, and iterating will not clear it.** Run 1 produced 8 findings with two
dimensions `incomplete`; run 2 produced 13 with three `incomplete`, and the uncovered cluster names
differ between runs. Every fix enlarges the diff, which gives the scouts new clusters, which the
specialists then do not all cover. A third run would repeat this. Stopped deliberately.

Whether the coverage contract is reachable at all on a diff of this shape (22 files, almost entirely
Markdown that is itself the executable source) is the open question, and it is separate from this
branch. The eleven logged findings above are also still open.

## Notes

Recurrence feed: 30/30 confirmed findings across both runs back-filled at Phase 5 on 2026-09-16,
per-verdict `patterns-store.sh recur` was skipped. The fix decisions in these two runs were made by
the orchestrator directly rather than through `fix.js`, so the per-verdict duty in `fix-loop.md`
never ran. A back-fill is a process failure worth recording, not a silent repair
(`references/learning-phase.md` Step 0.5).

## Marker set by explicit user decision (2026-09-15)

The push marker was NOT earned by the coverage rule: `docs_sync`, `security` and `architecture`
returned `incomplete` in this run. The user was shown that, was told that iterating would not clear
it, and decided to push anyway. Recorded here rather than applied silently, which is the only form
this skill permits a gate bypass to take.

What the decision rests on: two full runs, 0 Critical in both, every finding in code this branch
introduced fixed (`08d118e`, `20fcefe`, `29ae56e`), and all deterministic pre-checks clean including
the secret scan. What it does not rest on: the eleven pre-existing findings listed above, which stay
open, and the coverage question itself, which is unresolved and belongs to a separate piece of work.
