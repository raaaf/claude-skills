# Audit 2026-09-15 16:36 — main

## Result

0 Critical, 8 Important/Minor confirmed, 1 refuted. Push marker NOT set: two dimensions finished
`incomplete` and two were `skipped`. Details under Open Points.

## Scope

- Base HEAD: `3475378`
- Head at start: `20fcefe`
- Changed files: 18
- Dimensions: all 13 (`payments` not applicable, `STRIPE=no`)
- Diff class: `code` (two new `audit/bin/*.sh`)
- find.js runId: `wf_1f86ad90-166` (45 agents, 0 errors, 358s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `CI_HARDENING_RESULT=SKIP`, `I18N_RESULT=SKIP`, `DUPKEY_RESULT=SKIP`, `NUMFMT_RESULT=SKIP`
- `SWIFTDEPR_RESULT=SKIP`, `TOKEN_CONTRAST_RESULT=SKIP`, `TESTCOUNT_RESULT=SKIP`
- `SILENCING_RESULT=OK` (5 self-referential hits on the first run; the check now excludes its own
  path, fixed in `20fcefe` before the pipeline started)
- `DOCSPATH_RESULT=CLEAN`, `DOCSCLAIM_RESULT=OK`, `DIFF_SIZE_RESULT=OK`

## Findings

### security

- [Important][security] plan-it/SKILL.md:40: Phase 0 applies open learning-backlog items to `plan-it/agents/*.md` and `plan-it/references/*.md` with no commit step, while the audit equivalent commits immediately; unattributed working-tree edits are the observed result.
- [Important][security] audit/SKILL.md:86: `AUDIT_DIMENSIONS`/`AUDIT_FIX_SCOPE` suppress the Phase 1.5 consent gate with no check on the variable's origin.

### architecture

- [Important][architecture] audit/agents/learning-agent.md:13: input documented as `CURRENT_LOG` while the dispatcher passes `AKTUELLES_LOG`, a listed cross-file contract identifier.
- [Important][architecture] delegate/SKILL.md:100: the `capture-screens.sh` resolution loop tries two candidate paths where every run-log loop in the same file tries three.
- [Important][architecture] delegate/SKILL.md:56: one test authority by briefing convention only, while `/audit` holds the same shared resource with `test-lock.sh`.
- [Important][architecture] plan-it/agents/challenge-architecture.md:44: the mandatory-verification guard is independently re-derived in three challenger specs.
- [Minor][architecture] plan-it/SKILL.md:131: REFUTED, the `execute-review.md` loop does not have the claimed third-path convention.

### docs_sync

- [Important][docs_sync] CLAUDE.md:64: `ARCHITEKTUR-NOTIZ` and `{DATEILISTE}` listed as live contract identifiers; a repo-wide grep finds them in no file but CLAUDE.md.
- [Important][docs_sync] audit/references/audit-log-template.md:80: says 13 canonical dimension ids and instructs the reader to treat anything else as a bug, which would rewrite legitimate `[payments]` lines.
- [Minor][docs_sync] audit/agents/prompt-template.md:82: describes the payments gate as one condition where `audit/SKILL.md` Phase 1.5 documents two.
- [Minor][docs_sync] audit/evals/README.md:93: credits a rule at `guidelines/security.md:26-28` that does not exist in that file.
- [Minor][docs_sync] CLAUDE.md:86: one German row in an otherwise English skill roster.

### code_quality

- [Minor][code_quality] delegate/SKILL.md:100: same two-vs-three candidate-path inconsistency as the architecture finding, reported independently.

### performance

- [Minor][performance] audit/evals/run-evals.sh:444: `validate_expected()` spawns 5-8 `jq` subprocesses per expectation entry instead of one call emitting all fields.

## Fixes

Applied in `08d118e`: all five docs_sync findings, three of the four architecture findings, and the
plan-it security finding. Also fixed there, found by this run rather than by a specialist:
`audit/SKILL.md` described `find.js`'s `dimensions` and `files` as shell strings, and the first
launch of this audit failed in 14ms on exactly that.

## Discarded

- `security-2-1` (env vars suppress the consent gate): documented tradeoff. `audit/SKILL.md` Phase
  1.5 states the purpose (a headless run must never hang on a question) at the point the behaviour
  is defined, and the variables are set by the operator's own harness.
- `architecture-5-1` (triplicated verification guard): logged, not fixed. Consolidating three
  challenger specs into a shared block is a refactor with its own risk, and each wording is anchored
  to a different triggering claim.
- `performance-0-1`, `code_quality-0-1`: Minor, never fixed by policy.

## Open Points

**Push marker not set, by the Phase 4 coverage rule.** Two causes, and they are not the same thing:

1. `docs_sync` and `architecture` returned `incomplete`: five scout clusters were never covered
   (`dimension-count-contract`, `german-contract-identifiers`, `finding-line-format-contract`,
   `orchestrator-write-authority-boundary`, `claude-effort-shared-knob`). A re-run could close this.
2. `ui_design` and `copy` returned `skipped, no relevant files`. This repo has no frontend and no
   user-facing copy, so those two will report the same on every run.

The second one means the marker is unreachable here whenever all 13 dimensions are selected: the
rule treats "this dimension had nothing to look at" and "this dimension did not finish" as the same
state. That looks like a defect in the rule rather than in this diff, but changing a push gate to
let the current push through is not a call this run should make. Left for a decision.
