# Audit 2026-08-04 22:30 — main

Effort high (3 rounds, FixMinor=1, ConfidenceFloor=low). Scope: whole working tree, 45 files,
832 diff lines against `ecb1098`. Diff-size gate: LARGE (architecture + security escalated to Opus).

## Routing

Round 1: lief [security,code_quality,docs_sync,architecture,copy]; uebersprungen
[performance,seo,a11y,typography,ui_design,ux,animation: kein Laufzeit-/Frontend-Anteil im Diff];
Floor-Override [security:code-changed;code_quality:code-changed]

The floor was extended during this audit (see Important 4), so a rerun now routes
security, performance, code_quality and docs_sync from the floor alone.

## Pre-checks

`SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`,
i18n/dupkey/numfmt/swift/testcount all `SKIP` (not applicable to this repo).
`Validator: 19/19 verified, 0 hallucinated (discarded)`.

## Verification (Step D.7, first real run)

`Verification: 9 confirmed, 1 refuted, 0 uncertain (of 10)` in round 1, plus one more finding
verified in the cross-reference pass (confirmed with a severity correction).

Refuted before any file was touched:

- **[Architecture] audit/SKILL.md** — claim: `SAUBER` is derived from the confirmed count only, so a
  Critical `UNCERTAIN` would clear the Phase 4 push gate. Refuted: `SAUBER` only ends the fix loop.
  Every path runs through Phase 3, and 3f forces an explicit AskUserQuestion decision on the open
  point before Phase 4 writes the marker.

## Critical

None.

## Important (all fixed and verified)

1. **[Architecture] audit/SKILL.md:48** — the effort `case` matched only `high)`, so `xhigh` fell
   into the `medium|*)` catch-all and silently ran with 2 rounds and floor `medium`, contradicting
   the D.7 selection table and full-audit's `high|xhigh|*)`. Fixed to `high|xhigh)`, all effort
   parentheticals in the file aligned.
2. **[Docs] audit/references/audit-log-template.md, full-audit/references/audit-log-and-issues.md** —
   Step D.7 routes `UNCERTAIN` verdicts into an `### Unverified` section that existed in neither log
   template. Section added to both, between Open Points and Clean.
3. **[Architecture] audit/bin/check-skips.sh** — the floor left performance, seo, animation and
   docs_sync to the triage, but the triage became opt-in, so those four were skipped on every default
   run. Observed live in this audit's own round 1. Signals added (`has_docs`, `has_seo`, plus
   performance on code/migrations and animation on frontend), CLAUDE.md gotcha corrected.
4. **[Code-Quality] audit/evals/run-evals.sh** — six defects in one file: `must_not_find` matched the
   dimension raw while `must_find` built a separator- and synonym-tolerant pattern (false positives
   silently under-reported); `--timeout` accepted non-numeric values whose failure was swallowed;
   `--only`/`--timeout` read `$2` unguarded under `set -u`; timeout detection used wall clock instead
   of exit code 124; a missing `line` field became the string "null" and was treated as 0; an empty
   `matches` array made `grep -iE ""` match everything. All fixed, pattern construction extracted so
   the two scoring paths cannot drift again.
5. **[Code-Quality] review/SKILL.md** — base-branch resolution fell back to
   `git config --get init.defaultBranch`, which names the branch for future `git init` calls and says
   nothing about the current repo. Replaced with `audit/bin/lib-git-base.sh`, which already solved
   this. Verified against a throwaway repo on `master` with no origin: now resolves `master`.
6. **[Docs] README.md, audit/evals/README.md** — the eval section claimed 29 fixtures across 8
   categories; the real numbers are 51 scorable fixtures across 9, and the `docs` category was
   missing entirely. Both files corrected, the new runner options and the measured cost documented.
7. **[Code-Quality] audit/evals/run-evals.sh** — a directory-based fixture could never be scored:
   base names were derived per file, so `docs/test-count-drift/` never matched
   `expected/test-count-drift.json` and its expected JSON sat there as an orphan. Enumeration moved
   to one unit per fixture. Verified: 51/51 scorable, 0 orphans, single-file selection unchanged.
8. **[Architecture] audit/SKILL.md:355** — Step E.5 dispatched the fix-verifier as `code-reviewer`,
   which has no Bash, while `agents/fix-verifier.md` requires `general-purpose` precisely so the
   verifier can run the test suites itself (learning log 2026-07-09). Pre-existing, not introduced by
   this session. Fixed. The D.7 dispatch correctly stays on `code-reviewer`.
9. **[Architecture] design-audit/SKILL.md** — dispatches agents from the resolved audit directory but
   never ran `verify-agents.sh` against it, so a partial install failed mid-run instead of fast.
   Fail-fast call added.

## Minor (fixed)

- full-audit/SKILL.md round output and design-audit/SKILL.md report template had no Unverified
  section; design-audit was missing the "missing or unparseable verifier reply counts as UNCERTAIN"
  fallback that the other two skills define. All three added.
- audit/SKILL.md Phase 2.5 duplicated the cross-ref trigger bash verbatim from
  `references/cross-reference.md`; the reference is now the only copy.
- audit/evals/README.md still called the suite a scaffold with three example bugs, and its
  "add a fixture" instructions pointed at `expected/{category}/` although `expected/` is flat.
- design-audit's defect verification is deliberately not scaled by `CONFIDENCE_FLOOR`; that is now
  stated explicitly so nobody "fixes" it later.

## Minor (not fixed, deliberate)

- Several fix-wave lines carry em-dashes, against the repo convention "no em-dashes in new English
  prose where avoidable". Most of them edit sentences that already contained one. An automated sweep
  earlier in this session damaged unrelated in-flight work, so this stays a log entry rather than
  another fix wave.

## Unverified

None. Every finding that went through D.7 came back CONFIRMED or REFUTED.

## Notes

- Two learning-backlog items were implemented before the audit started, on request: the docs-sync
  checklist now covers every repeat site of a renamed or renumbered thing (CLAUDE.md tables,
  README counts, SKILL.md step numbering, plugin manifests), and `guidelines/architecture.md` gained
  section XVIII on lock, wait and heartbeat scripts (signal traps under `set -u`, orphan cleanup on
  kill, bounded waits). Both marked `[x]` in the learning log.
- This was the first execution of Step D.7. It behaved as designed: it refuted a plausible finding
  with a concrete control-flow trace, and corrected one severity from Critical to Important while
  also correcting the finding's own inaccurate claim about when the defect was introduced.
- 3a changelog: n/a, no CHANGELOG.md in this repo. 3b: `bash -n` clean on all three changed scripts.
  3c: the eval suite is the only executable test and costs roughly 7 USD per fixture, so it is not
  diff-scoped runnable; structural checks (`verify-agents.sh`, frontmatter parse of all 20 skills,
  line-count convention) were run instead and are green. 3d: n/a, no visual files in the diff.
