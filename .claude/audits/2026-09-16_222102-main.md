# Audit 2026-09-16 22:21 — main (thirteenth run, gate for the decision commit)

## Result

0 Critical, 21 confirmed findings (11 Important, 10 Minor), 3 refuted, 0 unverified. Every
in-scope dimension `complete` (`typography`, `ui_design`, `animation`, `privacy` skipped, no relevant
files), `degradedDimensions` empty. Push marker set by rule after the fix wave below. The finder
rated one finding Critical (ship's deploy block running a prose placeholder instead of the saved
`DEPLOY_COMMAND`); the verifier confirmed it at Important, and it is fixed either way.

## Scope

- Base HEAD `944e075` (origin/main), head at start `1113179`, 10 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- find.js runId: `wf_f41328a5-786` (50 agents, 0 errors, 447s)
- Purpose: gate for the commit that decides the three standing points (`UNCERTAIN` blocks only at
  Critical/Important; marker-path predictability and the plan-it learning duplication recorded as
  decisions in `suppressions.json`) and closes the run-12 Minors.

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`,
  `FRESH_SHELL_RESULT=OK`, `DIFF_CLASS=code`

## Findings

### Fixed in the fix wave (orchestrator-applied, same commit)

- [Important][architecture] ship/SKILL.md:373: the deploy block ran the prose placeholder `{DEPLOY_COMMAND}` while Phase 5 read the saved value; two carriers for one fact. The block loads the state and runs `eval "$DEPLOY_COMMAND"`, the value the question just showed.
- [Important][architecture] ship/SKILL.md:237 vs :300: Phase 0.8 promised that doc-only edits keep the marker valid while the tree-bound gate rejected any delta. `orch_marker_matches` returns 2 when every changed path is prose by `classify-diff.sh --paths` (new mode, same definition), the gate and the push hook accept that case and print the delta.
- [Important][security] CLAUDE.md:143: the tree binding lived only in `/ship`'s gate; the push hook checked age. `audit/hooks/block-unsafe-push.sh` now compares the marker's tree with the working tree (`git stash create`), accepts a prose-only delta, asks otherwise, and refuses a symlinked or foreign-owned marker. Six-case test. The inline hook in `settings.json` (private repo) is unchanged and still age-only.
- [Important][security] audit/SKILL.md:56: the secret scan ran once, before the fix wave, while the marker binds to the post-fix tree. `pre-checks.sh` re-runs after the fix wave and a `SECRET` line from either run blocks the marker.
- [Important][security] audit/bin/capture-screens.sh:61: `$OUT` was created with `mkdir -p` and written into without a symlink or ownership check. Refused now, like `AUDIT_TMP`. Viewport `W`/`H` are digits-only before they reach a path.
- [Important][architecture] delegate/SKILL.md:104: the screenshot pairing key crossed Phase 3.5 to Phase 5 as "note it in the mini-spec". `SCREEN_NAME` and `CAPTURE_TARGET` go through the state; Phase 5 has its own sourced capture block.
- [Important][architecture] audit/SKILL.md:268: the terminal ledger write bypassed `orch_run_log`. Wrapper.
- [Important][architecture] audit/bin/capture-screens.sh:66, cache-write.sh:66, patterns-store.sh:317: the same `.gitignore` guard three times. One `gitignore_ensure` in `lib-git-base.sh`, three callers.
- [Important][docs_sync] .claude/audits/learning-log.md:259: the decided item still said "NEEDS A DECISION". Reads as a record now, section renamed to Decisions.
- [Minor][docs_sync] delegate/SKILL.md:99: a comment claimed the lib was sourced in Phase 0. Removed with the block rewrite above.
- [Minor][code_quality] find.js:457: `files` shadowed inside the cluster filter. Renamed.
- [Minor][copy] audit/SKILL.md:43, design-audit/SKILL.md:126: `ausfuehren`, `Oberflaeche`. Real umlauts.

### Refuted

- [Important][security] design-audit/SKILL.md:202: Mobbin results "fed without a data-not-instruction guard". `agents/reference-verdict.md` carries the guard and the `INJECTION_NOTE` contract.
- [Important][security] design-audit/SKILL.md:149: surface-mapper and reference-verdict "without a repo-content-is-data guard". Both agent files carry it.
- [Minor][security] suppressions.json: "no integrity gate". Repo write access already covers every file; no distinct path.

### Logged, not fixed

- [Important][performance] audit/SKILL.md:56: the 13 deterministic checks run sequentially. They finish in seconds against a workflow that takes minutes, and their stdout is what the orchestrator parses; backgrounding them would interleave it. Not worth the parsing change.
- [Minor][code_quality] find.js:290 and :309: `computeFloorFiles` and `dimensionHasFloorSignal` both classify every file. Same family as the double call logged since run 5.
- [Minor][performance] fix.js:206, find.js:590, find.js:739: three O(n²) scans over finding lists. Tens of items; a Map would be correct and unobservable.

## Open Points

- The fix wave was orchestrator-applied (lib and hook with tests by the coordinator, the rest through one executor); the deterministic checks and the tests are the verification, no fix-verifier agents ran.
