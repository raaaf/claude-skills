# Audit 2026-09-16 18:57 — main (ninth run, gate attempt after run-8 fixes)

## Result

1 Critical, 17 confirmed findings, 1 refuted, 0 unverified. Every in-scope dimension `complete`
(`typography`, `ui_design`, `animation` skipped, no relevant files). Push marker NOT set by rule:
one Critical open, the second site of the class run 8 fixed (finding text spliced into a command
line), fixed in the follow-up commit with the same file-driven design.

## Scope

- Base HEAD `531f03c` (origin/main), head at start `e148ad6`, 24 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- find.js runId: `wf_d0a5503e-cde` (45 agents, 0 errors, 572s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`,
  `FRESH_SHELL_RESULT=OK`, eval validator 0 errors

## Findings

### Fixed in the follow-up commit

- [Critical][security] design-audit/SKILL.md:213: Phase 3 spelled out `patterns-store.sh recur {pattern}` and `dismissed {pattern}` with the verifier's pattern text on the command line. Patterns are appended to `{AUDIT_TMP}/recur.txt` / `dismissed.txt` with the Write tool and fed by `orch_patterns_from_file`, which passes each line as one argv element; tested with quotes, `$(...)` and semicolons in the pattern. `learning-phase.md`'s back-fill instruction had the same shape and uses the same call.
- [Important][docs_sync] audit/bin/lib-orchestrator.sh:19 and full-audit/SKILL.md:32: the header claimed full-audit always adds a `CLAUDE_PROJECT_DIR` candidate; only its Phase 0 loop did, and that variable exists only in hooks. Candidate removed, all six skills use one loop, header says so.
- [Important][docs_sync] design-audit/SKILL.md:157 and :275: both marker touches were prose. Fenced, sourced blocks.
- [Important][architecture] audit/SKILL.md:216: the Phase 3 touch was prose while Phase 2's is a block. Fenced.
- [Important][security] design-audit/agents/*.md: the `INJECTION_NOTE` line had no consumer. Phase 3 step 4 turns each into an `[Important][security]` finding.
- [Important][security] ship/SKILL.md:374: `HEALTH_URL` curled unfiltered, named in six runs. Re-derived in its block, http(s) only, loopback/private/link-local hosts refused.
- [Important][docs_sync] CLAUDE.md:88: `/ship` roster row omitted the test gate. Added.
- [Minor][code_quality] audit/SKILL.md:234: Phase 4 set `AUDIT_BIN` from `CLAUDE_SKILL_DIR` alone while every other phase resolves it through the lib's fallbacks. Same call now.
- [Minor][docs_sync] audit/SKILL.md:254: cited `find.js:405-427` for a rule that lives at 434. Cites the symbol and the expression, not a line.
- [Minor][docs_sync] CLAUDE.md:56: the dupes-check row named four symbols, the script guards five. Five.
- [Minor][copy] plan-it/SKILL.md:268 and design-audit/SKILL.md:121: one German heading in an English output spec, one English echo next to a German exit message. Each block in one language.

### Refuted

- [Important][security] find.js:509: `PROJECT_GUIDELINES` "spliced verbatim with stated precedence". That is the documented contract (`scope-and-pre-checks.md`, trust boundary paragraph, run 7); the finding restates the accepted residual.

### Logged, not fixed

- [Important][security] audit/bin/lib-orchestrator.sh:44: marker paths predictable from `md5(cwd)`. Seventh run naming it; single-user `/tmp` convention, unchanged.
- [Minor][performance] audit/workflows/find.js:360 and :823: `computeFloorFiles` twice per dimension. Sixth run. Left alone: a caching change in the core pipeline for a Minor is not worth the risk today.

## Open Points

- Whether one `UNCERTAIN` verdict should block the marker (runs 5 and 7). Design decision, still open.
- `check-fresh-shell.sh` covers functions, not variables crossing blocks. Not built.
