# Audit 2026-09-10 — main

## Ergebnis

5 Findings, alle gefixt. 0 Critical offen. **Push-Marker NICHT gesetzt** (Begruendung unten).

## Scope

- BASE_REF 7ea6538, 32 geaenderte Dateien (payments-Dimension: Detector, Agent, Guideline, Verdrahtung, Evals)
- FRAMEWORK=generic, PLATFORM=web, STRIPE=no (dieses Repo hat keine Stripe-Integration)
- DIFF_CLASS=code, DIFF_LINES=1137, DIFF_SIZE_RESULT=LARGE

## Abweichung vom Standardlauf

Die find.js/fix.js-Pipeline wurde NICHT dispatcht. Stattdessen ein manueller Durchgang durch die
geaenderten Dateien durch das Orchestrator-Modell. Grund: die Pipeline verlangt den vollstaendigen
Inhalt aller 32 Scope-Dateien (132 KB) als Inline-Argument; derselbe Mechanismus hat am selben Tag
im shop-Repo eine Session dazu gebracht, die Pipeline zu umgehen. Der manuelle Durchgang fand fuenf
Defekte, zwei davon still versagend. Vollstaendig gelesen: find.js, detect-stripe.sh, audit/SKILL.md,
guidelines/payments.md, agents/14-payments.md, references/dimension-selection.md,
full-audit/SKILL.md, der geaenderte Teil von run-evals.sh. Nicht gelesen: der unveraenderte Teil von
run-evals.sh, evals/README.md, die 12 Fixture-PHP-Dateien (Testdaten, laut CLAUDE.md nie Findings).

## Mechanische Checks

| Check | Ergebnis |
|---|---|
| Secret-Scan | CLEAN |
| Lockfile-Drift | CLEAN |
| Binaerartefakte | CLEAN |
| CI-Hardening | SKIP (kein .github/workflows) |
| Doc-Claims | 6 Treffer, davon 5 vorbestehend (plan-it/SKILL.md, CLAUDE.md:73) |
| verify-agents | OK (nach Fix) |

## Findings

### code_quality

- **audit-1-1 (Critical, high)** — `audit/SKILL.md:44,57` und `full-audit/SKILL.md:53` nutzten
  `sed -n '...{...;p}'`. BSD sed (macOS) lehnt `p}` ohne Trenner ab. Folge: `ALLE_DATEIEN` und
  `STRIPE_FILES` still leer, die payments-Dimension waere in jedem Lauf uebersprungen worden, weil
  die Diff-Schnittmenge leer bleibt. **FIXED** (`p;}`, gegengeprueft: 32 bzw. 50 Dateien).
- **audit-1-2 (Critical, high)** — `audit/SKILL.md:151` uebergab im `Workflow`-Aufruf nur
  `STRIPE_MODE`, nicht `STRIPE_RECURRING`, entgegen dem eigenen Prosa-Absatz auf Zeile 125 und
  entgegen `agents/14-payments.md`, das beides verlangt. Die Recurring-Gatung der Dashboard-Punkte
  waere in `/audit` wirkungslos geblieben. `full-audit/SKILL.md:130` war korrekt. **FIXED.**

### docs_sync

- **audit-1-3 (Important, high)** — `audit/bin/verify-agents.sh` fuehrte `14-payments.md` nicht in
  `REQUIRED`. Die Pruefung meldete OK, weil sie nur die Liste abhakt und zusaetzliche Dateien nicht
  bemerkt; ein Verlust der Datei waere unentdeckt geblieben. **FIXED.**
- **audit-1-4 (Important, high)** — `CLAUDE.md:31,39,79,115` nannten weiter 13 Dimensionen und
  20 Agent-Dateien. **FIXED** (14 Dimensionen konditional, 21 Agent-Dateien).
- **audit-1-5 (Minor, high)** — `audit/references/dimension-selection.md:60` schrieb die Display-
  Zeile fest auf `{N}/13 dimensions`. **FIXED.**

## Offene Punkte

1. `audit/bin/detect-stripe.sh` nutzt an sechs Stellen `printf '%s\n' "$LISTE" | xargs grep -l`.
   Dateinamen mit Leerzeichen zerfallen dabei in mehrere Argumente, moeglicher stiller Fehltreffer
   in der Payment-Oberflaeche. Nicht gefixt, gehoert in einen eigenen Durchgang mit `-print0`/`-0`.
2. Die payments-Dimension ist weiterhin nie durch `find.js` gelaufen. Der einzige echte Lauf
   (shop, 2026-09-10 15:49) hat die Pipeline umgangen. Der naechste `/audit` in shop oder events
   ist der ausstehende Beweis fuer `dimensionFiles`/`dimensionContext`.
3. Die Eval-Fixtures unter `audit/evals/fixtures/payments/` sind nie gelaufen. Recall und
   Falsch-Positiv-Rate der Dimension sind unbekannt.
4. Vorbestehend, ausserhalb dieses Diffs: `plan-it/SKILL.md:210-213` referenziert vier nicht
   existierende `agents/challenge-*.md`, `CLAUDE.md:73` einen nicht aufloesenden Pfad.

## Push-Gate

Marker nicht gesetzt. Die Dimensionsauswahl war vollstaendig, aber die Pipeline lief nicht, und ein
manueller Durchgang erfuellt das Gate nicht. Ein Push braucht damit eine ausdrueckliche Entscheidung
des Nutzers, die hier protokolliert gehoert.

## Gate-Entscheidung

2026-09-10, ausdrueckliche Nutzerentscheidung: Push-Marker wird gesetzt und gepusht, obwohl die
find.js-Pipeline nicht lief. Grundlage ist der manuelle Durchgang oben (fuenf Findings, alle gefixt,
davon zwei still versagende Defekte), nicht ein Pipeline-Lauf. Die vier offenen Punkte bleiben offen.
