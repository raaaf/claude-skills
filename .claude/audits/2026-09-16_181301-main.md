# Audit 2026-09-16 18:13 — main (sixth run, gate attempt after run-5 fixes)

## Result

2 Critical, 21 confirmed findings, 1 refuted. Every dimension with files in scope `complete`
(`typography`, `ui_design`, `animation`, `privacy` skipped, no relevant files). Push marker NOT set
by rule: two Criticals open, both in text this branch wrote today, both fixed in the follow-up
commit.

## Scope

- Base HEAD `531f03c` (origin/main), head at start `01e80e3`, 18 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- find.js runId: `wf_0e7bb088-2dc` (49 agents, 0 errors, 649s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`

## Findings

### Fixed in the follow-up commit

- [Critical][architecture] delegate/SKILL.md:97, ship/SKILL.md:271, plan-it/SKILL.md:317, design-audit/SKILL.md:81, audit/SKILL.md:281: seven Bash blocks called `orch_*` functions in a shell that had never sourced the lib. Every Bash block is a fresh process; the pre-refactor skills re-declared `AUDIT_BIN` in Phase 4 for exactly this reason and the refactor removed that. Every such block now begins with the one-line source loop, the prose sites say so, the lib header states the rule, and CLAUDE.md carries it as a gotcha.
- [Critical][architecture] audit/SKILL.md:202 and ship/SKILL.md:48: two hand-written parsers for `.claude/ship.md` `test-command:` (`sed` strip vs `grep | cut -d' '`) that disagree on tabs and trailing spaces. One parser now, `orch_test_command_declared`, with `orch_test_command` adding `/audit`'s manifest fallback; `/ship` deliberately stays declared-only.
- [Important][code_quality] audit/SKILL.md:69: `PROJECT_GUIDELINES` read in Phase 1 and passed nowhere, so `.claude/audit-guidelines.md` never reached a specialist despite CLAUDE.md promising precedence. `find.js` takes `projectGuidelines` and appends it to every specialist and verifier briefing; Phase 2 passes it.
- [Important][code_quality] audit/workflows/find.js:165: `ROOT_HEADER` duplicated in `fix.js` but not in the dupes guard. Guarded; the two had already drifted in their comment and are aligned.
- [Important][architecture] design-audit/SKILL.md:152: marker touched after the audit wave only, not after the Phase 6 fix wave as `/audit` does. Touch added.
- [Important][security] design-audit/agents/reference-verdict.md:27: the injection guard named repo content only, while `REFERENCE_STRUCTURE` arrives from a third-party tool. Guard extended to tool output.
- [Important][docs_sync] CLAUDE.md:148: the marker gotcha still said `/design-audit` claims per dispatch. Updated to today's model.
- [Minor][docs_sync] CLAUDE.md:31: payments described as gated on `STRIPE=yes` alone; the diff must also touch the Stripe surface. Stated.
- [Minor][docs_sync] CLAUDE.md:124: `check-skips.sh` described in the present tense as live routing. Past tense, pointer to the Commands row.
- [Minor][docs_sync] audit/workflows/find.js:208: two comments cited a CLAUDE.md gotcha by a title renamed on 2026-09-06. Current title.
- [Minor][docs_sync] audit/bin/lib-orchestrator.sh:17: header presented one guard as universal; delegate/ship/plan-it guard on `orch_run_log`. Header explains the two guards and why.
- [Minor][docs_sync] plan-it/SKILL.md:87: comment claimed Phase 4 does its own lookup; it did not, which was the Critical above in one file. Comment rewritten with the fix.
- [Minor][copy] audit/SKILL.md:43, design-audit/SKILL.md:43: the three sibling fatal messages in three wordings and two languages. All German, same shape.

### Refuted

- [Important][security] audit/bin/lib-orchestrator.sh:70: `orch_verify_agents` "only checks presence". That is `verify-agents.sh`'s documented contract (roster completeness), not a gap; content trust is the worker hard rules' job.

### Logged, not fixed

- [Important][security] audit/bin/lib-orchestrator.sh:42: marker path predictable, no ownership check. Fourth run naming it; single-user `/tmp` convention.
- [Important][security] design-audit/SKILL.md:202: `patterns-store.sh recur {pattern}` with finding-derived text. The pattern is normalised by `normalize-suppression.sh` and passed as one quoted argument; no shell evaluation of its content. Noted for the next design-audit pass to confirm the quoting at the call site.
- [Minor][code_quality] audit/SKILL.md:162 and full-audit/SKILL.md:136: payments-floor jq merge pasted in both. Candidate `orch_payments_floor`; next pass.
- [Minor][architecture] audit/workflows/find.js:83: "Duplicated in fix.js" comment convention applied to two of five guarded symbols. Cosmetic.
- [Minor][performance] audit/workflows/find.js:350: `computeFloorFiles` twice per dimension. Fourth run naming it.

## Open Points

- Whether one `UNCERTAIN` Minor should block the push marker (run 5) is still a design decision,
  not made here.
