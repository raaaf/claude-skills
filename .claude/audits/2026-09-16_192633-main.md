# Audit 2026-09-16 19:26 — main (eleventh run, gate attempt after run-10 fixes)

## Result

1 Critical, 12 confirmed findings, 0 refuted, 0 unverified. Every in-scope dimension `complete`
(`typography`, `ui_design`, `animation` skipped, no relevant files). Push marker NOT set by rule:
one Critical open, the third instance of the variable-crossing-blocks class (runs 8, 10, 11). The
follow-up commit builds the mechanism for the class instead of patching the instance.

## Scope

- Base HEAD `531f03c` (origin/main), head at start `4d4f91d`, 28 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- find.js runId: `wf_b7f2aaae-c9f` (46 agents, 0 errors)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`,
  `FRESH_SHELL_RESULT=OK`, eval validator 0 errors

## Findings

### Fixed in the follow-up commit

- [Critical][security] audit/SKILL.md:104 and full-audit/SKILL.md:117: the Phase 1.5 block read `STRIPE`, `ALLE_DATEIEN`, `STRIPE_FILES` and `GUIDELINE_MATCHES` from Phase 1's block, so `${STRIPE:-no}` was always `no` and `payments` could never join a run. Class fix: `orch_state_save`/`orch_state_load` in `lib-orchestrator.sh` (one file per variable under a 0700 dir keyed like the in-progress marker, raw bytes back through `read -r -d ''`, nothing eval'd; `orch_progress_claim` clears it). Every block that produces a carried value saves it, every reader loads it, in all six skills and the references; `check-fresh-shell.sh` now enforces the variable half too (a read of an unset uppercase name without a load, a name nobody saves, a save of a name the block never set), scanning `*/SKILL.md` and `*/references/*.md`. Seven such blocks existed; the same scan also showed the dimension answer never reached the shell at all (`AUDIT_DIMENSIONS` built from an env var that is unset in an interactive run), now an explicit assignment from the answer.
- [Important][architecture] ship/SKILL.md:178: every `SHIP_*` value was set in one block and read as `${SHIP_GATE:-n/a}` in another, so the ledger recorded `n/a` for every ship gate. Same mechanism; each assignment is a sourced block ending in a save, the log call loads.
- [Important][architecture] audit/SKILL.md:259 and ship/SKILL.md:288: the passed marker was an empty file whose mtime was the whole claim. `orch_marker_write` records the audited tree (`git stash create`, tracked working-tree content; HEAD's tree when clean) and `/ship`'s gate requires `orch_marker_matches` after its commit in addition to the age. The PreToolUse hook keeps checking age only, it has no run context.
- [Important][ux] ship/SKILL.md:346: with `DEPLOY_COMMAND=ci` Phase 5 curled the health URL while the CI deploy was still running. Phase 5 now finds the run for the branch and `gh run watch`es it; a non-success CI result skips the health check and goes to Phase 6.
- [Important][security] ship/SKILL.md:378: the host filter matched dotted-decimal globs only; decimal, octal, hex and short numeric forms of loopback passed. `orch_url_host` and `orch_host_public` in the lib: a numeric host must be a canonical dotted quad with every octet in range before the private ranges are checked, hex is refused outright; 21-case test.
- [Important][architecture] full-audit/SKILL.md:98: touch and release existed only as prose. The prose now says what is true: Phases 2-5 are `audit/SKILL.md`'s blocks, which carry them; the claim stays in Phase 0's block.
- [Minor][code_quality] find.js:343, fix.js:129: the trailing-chunk merge had no upper bound. Merge only when the result stays within 8 (find) or 7 (fix) files.
- [Minor][docs_sync] CLAUDE.md:106: the broken sentence. Fixed.

### Logged, not fixed

- [Important][architecture] plan-it/SKILL.md:335: the learning dispatch duplicates `audit/references/learning-phase.md` and lacks its Step 0/0.5. Named in five runs; a shared learning phase across two skills with different log formats is a plan-it session, not a fix-wave edit.
- [Important][security] audit/bin/lib-orchestrator.sh:56: marker paths predictable from `md5(cwd)`. Ninth run naming it; single-user `/tmp` convention. The new state dir applies the ownership and symlink checks this finding asked for; the two markers do not.
- [Important][performance] audit/bin/patterns-store.sh:175: one process and one full store rewrite per pattern in `orch_patterns_from_file`. Correct, small N in practice; a batch mode is a separate change.
- [Minor][performance] find.js:362 and :825: `computeFloorFiles` twice per dimension. Seventh run. Left.

## Open Points

- The `UNCERTAIN`-blocks-marker decision: see `learning-log.md`, "Open decision".
- Variables crossing blocks are now mechanically checked; the mechanism is one run old. Run 12 is its first gate.
