# Audit 2026-09-16 15:08 — main (fourth run, gate attempt)

## Result

0 Critical, 22 confirmed findings (14 Important, 8 Minor). `status: complete` for every dimension
that had files in scope; `ui_design` and `copy` `skipped, no relevant files`. Push marker SET by
rule: no Critical open, no `incomplete`, `degradedDimensions` empty, no test suite in this repo so
no baseline to regress against. First run on this branch to earn the marker rather than have it
set by override.

Both coverage fixes held: zero per-chunk coverage failures logged (specialist briefing, `4265a58`)
and zero cluster rejections logged (zero-count files allowed, `32c46ea`).

## Scope

- Base HEAD `3475378`, head at start `32c46ea`, 23 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- find.js runId: `wf_053add12-cc5` (46 agents, 0 errors, 508s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, eval validator 0 errors
- `TEST_COMMAND=<none>` (Phase 3 derivation, this repo has no suite)

## Findings

### Fixed in the follow-up commit

- [Important][docs_sync] audit/workflows/fix.js:229: regression pass told to check "all 13 dimensions", excluding `payments` on a Stripe repo. Now the selected set.
- [Important][docs_sync] audit/references/learning-phase.md:15: Step 0.5 located the per-verdict `recur` duty in `fix-loop.md`, deleted 2026-09-05; line 25 (written 2026-09-16) repeated it. Both point at `fix.js`/`fix-agent.md`.
- [Minor][docs_sync] audit/references/learning-phase.md:3: skip condition named `SKIP_LEARNING`, which only plan-it sets; audit tests `CLAUDE_EFFORT=low` directly. Stated.
- [Minor][docs_sync] CLAUDE.md:96: the Effort table did not say `/plan-it` defaults to `xhigh`. Header says so.
- [Minor][code_quality] full-audit/SKILL.md:39: `AUDIT_REFS` assigned, never read. Removed.
- [Important][security] audit/SKILL.md:193: Phase 3 runs a repo-supplied `test-command:`, the third such site after `perf-measure:` and `/ship`. Accepted with the same reasoning as `/ship`'s test gate and added to the CLAUDE.md gotcha that counts these sites.

### Logged, not fixed

- [Minor][code_quality] audit/SKILL.md:47: `FRAMEWORK`, `SOURCE_DIRS`, `PLATFORM` assigned in Phase 1 and not read again in this file. Left in place: they document what the run detected and `detect-framework.sh` is cheap; removing them without a sweep of the references is not worth the risk today.
- [Important][security] design-audit/SKILL.md:138: mapping and reference-verdict agents dispatched as bare `general-purpose` with inline briefings. Same as run 3; separate work.
- [Important][security] design-audit/SKILL.md:56: `/tmp` marker paths derived from `md5(pwd)`, no ownership check. Same class as run 3's audit finding; single-user machine convention, noted.
- [Important][architecture] delegate/SKILL.md:41, ship/SKILL.md:168, design-audit/SKILL.md:38, audit/SKILL.md:75, full-audit/SKILL.md:140: the orchestrator prologue (helper resolution, cwd-hash at eight sites, payments-floor merge, run-log resolution repeated inside one file) pasted with variations. Same one condition as run 3's nine findings; deferred as one refactor, a shared `lib-orchestrator.sh`.
- [Important][architecture] design-audit/SKILL.md:56 and :154: the in-progress marker is described as run-scoped in Phase 0 and released unconditionally in Phase 4, and Phase 2 says to re-claim it before dispatches that Phase 2.5 does not perform. `/design-audit` still uses the pre-rebuild per-dispatch model; its marker text needs the same run-scoped rewrite `/audit` got on 2026-09-05. Separate work.
- [Important][architecture] audit/workflows/find.js:83 vs fix.js:47: `hasCompleteCoverage` duplicated under the no-import constraint and already diverged (fix.js additionally checks `Array.isArray(result.files)`). Worth one shared definition the Workflow tool can load; separate work.
- [Important][architecture] plan-it/SKILL.md:340: `plan-learning-agent` reimplements the audit learning contract. Third run naming it.
- [Minor][performance] audit/workflows/find.js:812 and :350: `computeFloorFiles` called twice per dimension with identical arguments. Cache the first result; small.
- [Minor][performance] audit/workflows/find.js:705: `dedupeFindings` O(n²) via `findIndex`. Same as run 3; harmless at current sizes.

## Open Points

- The architecture-duplication family remains the largest open item (now named in three runs). One refactor, not piecemeal.
- `/design-audit` still carries the pre-rebuild per-dispatch marker model.
