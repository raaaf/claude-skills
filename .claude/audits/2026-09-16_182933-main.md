# Audit 2026-09-16 18:29 — main (seventh run, gate attempt after run-6 fixes)

## Result

2 Critical, 20 confirmed findings, 1 unverified, 2 refuted. `security` `incomplete` (one
`UNCERTAIN` verdict), all other in-scope dimensions `complete`. Push marker NOT set by rule. Both
Criticals are in text this branch wrote today; both fixed in the follow-up commit, and the class
behind the first one now has a deterministic check.

## Scope

- Base HEAD `531f03c` (origin/main), head at start `41e2853`, 19 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`); `typography`, `ui_design`,
  `animation`, `privacy` skipped, no relevant files
- find.js runId: `wf_6b6e11d5-cae` (48 agents, 0 errors, 608s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`
- The inline fresh-shell scan reported 0, and was wrong (see Critical 1).

## Findings

### Fixed in the follow-up commit

- [Critical][security] ship/SKILL.md:289: the Phase 2 audit-gate block builds `MARKER` from `orch_hash_passed` without sourcing the lib in that block, so the hash is empty and the gate always reports MISSING. The inline scan that certified "0 unsourced blocks" in run 6 accepted a comment mentioning the file name as a source. Block sourced; the scan is now `audit/bin/check-fresh-shell.sh`, which recognises only the actual sourcing forms and runs in Phase 1. It also caught a second unsourced block this run's specialists did not: the payments-guideline block in `audit/SKILL.md`.
- [Critical][architecture] design-audit/SKILL.md:99: Phase 1 hand-rolled its own frontend extension regex while CLAUDE.md states there is exactly one definition (`FRONTEND_EXT_RE`, `lib-git-base.sh`); the two had drifted. `orch_frontend_ext_re` in the lib serves the shared pattern (`styl` added to it, `tailwind.config` stays a design-only addition at the call site).
- [Important][architecture] full-audit/SKILL.md:98: the in-progress claim existed only as prose; `orch_progress_claim` is now a line in the Phase 0 block.
- [Important][architecture] audit/SKILL.md:162 and full-audit/SKILL.md:136: the payments-floor merge and the payments-guideline append, pasted in both orchestrators and named in four runs, are `orch_payments_floor` and `orch_payments_guidelines`.
- [Important][architecture] audit/SKILL.md:191: the Phase 2 marker touch was prose; it is a fenced block with the source line, like every other call site.
- [Important][security] design-audit/SKILL.md:198: the hallucination-validator template interpolated `{datei}`/`{zeile}` from finding text bare into a shell command. Assigned first, shape-checked (integer line, no absolute or `..` path), then used quoted.
- [Minor][copy] audit/SKILL.md:43: "Audit abgebrochen" vs "Abgebrochen" across the three siblings. One prefix.
- [Important][security] audit/references/scope-and-pre-checks.md:97: `DECIDED_TRADEOFFS` and `PROJECT_GUIDELINES` are repo-authored and can make findings disappear. UNCERTAIN in verification for lack of a stated boundary; the boundary is stated now (owner's decisions, same trust as `CLAUDE.md`; a hostile first-time repo is the accepted residual because the same repo can lie in its code more easily).

### Refuted

- [Minor][docs_sync] CLAUDE.md:31: "payments missing from the agentType enumeration". It is present since `01e80e3`.
- [Important][architecture] audit/bin/check-workflow-dupes.sh:32: "the symbol list is hand-maintained". It is; a list that names what must stay identical is the check's contract, not its defect.

### Logged, not fixed

- [Important][security] audit/bin/lib-orchestrator.sh:50, audit/SKILL.md:277, ship/SKILL.md:289: marker paths predictable from `md5(cwd)`, existence and mtime only. Named in five runs now; single-user `/tmp` convention.
- [Important][security] ship/SKILL.md:47: `HEALTH_URL` from repo config, curled unfiltered. Fifth run.
- [Minor][code_quality] audit/workflows/find.js:215: `privacy` pushed by `TRANSLATION_RE` but absent from `SELECTIVE_FLOOR_DIMENSIONS`, so the signal suppresses a diagnostic without producing a floor file. Real, small; decide whether privacy should get a path floor before touching it.
- [Minor][code_quality] audit/workflows/find.js:728: `dimensionFileName` keeps two hand-maintained maps over the same ids. Derive `slugs` from `ALL_DIMENSIONS` on the next find.js pass.

## Open Points

- Whether one `UNCERTAIN` verdict should block the marker (runs 5 and 7). Design decision, still open.
