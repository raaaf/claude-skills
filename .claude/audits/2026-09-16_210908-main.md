# Audit 2026-09-16 21:09 — main (twelfth run, gate attempt after run-11 fixes)

## Result

0 Critical, 24 confirmed findings (13 Important, 11 Minor), 0 refuted, 0 unverified. Every
in-scope dimension `complete` (`typography`, `animation` skipped, no relevant files),
`degradedDimensions` empty. Push marker set by rule after the fix wave below: no Critical, no
`incomplete`, no `SECRET` line, no regression. First run since run 4 to pass the gate, and the
first run whose Phases 1, 1.5 and 2 carried their values through the state dir.

## Scope

- Base HEAD `531f03c` (origin/main), head at start `fa64f1d`, 34 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- find.js runId: `wf_4dd83394-a02` (49 agents, 0 errors, 535s)
- Purpose: first gate with the cross-block state mechanism (`orch_state_save`/`orch_state_load`),
  the tree-bound push marker and the extended `check-fresh-shell.sh`. Phases 1, 1.5 and 2 were run
  as separate shells and every carried value came back through the state dir.

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`,
  `FRESH_SHELL_RESULT=OK` (functions and variables), eval validator 0 errors, `DIFF_CLASS=code`

## Findings

### Fixed in the fix wave (orchestrator-applied, same commit)

- [Important][security] audit/bin/lib-orchestrator.sh:305: `orch_host_public` compared case-sensitively; `LOCALHOST` and `[FC00::1]` passed. Host lowercased first; six more test cases.
- [Important][security] audit/bin/lib-orchestrator.sh:103 and :173: the two marker files were touched, written and read with no symlink or ownership check while the state dir in the same file had one. Every marker access now refuses a symlink or a file another user owns (`orch__marker_ok`); release removes a planted symlink instead of following it. Reported twice (near-duplicate), one fix. The path predictability itself stays as logged below.
- [Important][security] CLAUDE.md:128 and audit/references/scope-and-pre-checks.md:58: both claimed `NAME=(...)` syntax alone makes `eval "arr=($d)"` safe. Verified false (`$(touch x)` inside the literal runs); the safety is `detect-framework.sh`'s per-element `%q`. Text corrected at both sites.
- [Important][security] design-audit/SKILL.md:195: w3-frontend was told to skip security while its shared template says to report injection as a security finding. It now returns an `INJECTION_NOTE:` line, which Phase 3 step 4 already consumes.
- [Important][architecture] audit/SKILL.md:171 and :257, full-audit/SKILL.md:155, design-audit/SKILL.md:86: four `exit 1` paths after the in-progress claim left the marker behind. Each releases first.
- [Important][architecture] design-audit/SKILL.md:107: `FRONTEND_FILES` computed and never saved. Added to the Phase 1 save.
- [Important][architecture] design-audit/SKILL.md:276: cited `/audit` "Phase 2 E/E.5", a stage gone since 2026-09-05. Cites Phase 3 (`fix.js`).
- [Important][architecture] design-audit/agents/*.md vs agents/design-*.md: `model`/`maxTurns` declared in both the worker spec and the real frontmatter. The spec now points at the frontmatter; one source.
- [Important][docs_sync] audit/references/scope-and-pre-checks.md:145: the `check-fresh-shell.sh` row described only the function hit. The two variable hit kinds and their fix are in the row.
- [Important][docs_sync] .claude/audits/learning-log.md:9: "9 raw logs on disk" was carried over from the 2026-09-10 retro; recounted from disk.

### Logged, not fixed (Minor is never fixed; the security one is the standing decision)

- [Important][security] audit/bin/lib-orchestrator.sh:76: marker paths predictable from `md5(cwd)`. Tenth run naming it; the ownership and symlink guard above is the part that was actionable, the path stays a single-user `/tmp` convention.
- [Minor][security] design-audit/SKILL.md:56: `AUDIT_TMP` refuses a symlink but not a pre-existing directory owned by someone else. Next design-audit pass: use `orch__state_dir_ok`'s check.
- [Minor][docs_sync] CLAUDE.md:90: `/delegate` roster cell says `inherit` while the frontmatter deliberately omits `model:`. Wording.
- [Minor][code_quality] audit/workflows/fix.js:142: `args.promptDir` fallback is dead (no caller passes it). Harmless.
- [Minor][code_quality] audit/workflows/find.js:296: the over-cap floor log line fires twice per dimension, the same double `computeFloorFiles` call logged since run 5.
- [Minor][architecture] audit/SKILL.md:109 and full-audit/SKILL.md:124: the 13-id literal is pasted in both Phase 1.5 blocks. A lib helper would be the third copy of `ALL_DIMENSIONS` (`find.js`, `lib-git-base.sh`), decide once which is canonical before adding it.
- [Minor][architecture] find.js/fix.js: three of the five guarded duplicate symbols lack a "duplicated in" comment. `check-workflow-dupes.sh` is the guard, the comment is nicety.
- [Minor][performance] audit/bin/validate-locations.sh:25: stat and `wc -l` per row, not per unique path. Small N.
- [Minor][copy] audit/SKILL.md:92 vs dimension-selection.md:25: option labels spelled two ways ("All" vs "Everything (default)"). Align on the next Phase 1.5 edit.
- [Minor][copy] audit/SKILL.md:44: "audit root" vs "audit-Root" in the three siblings. One spelling next pass.
- [Minor][copy] ship/SKILL.md:306 vs :308: "Audit marker" vs "the audit marker" after the same `STALE:` prefix.

## Open Points

- The `UNCERTAIN`-blocks-marker decision: see `learning-log.md`, "Open decision".
- The fix wave was orchestrator-applied (a lib change with a test, doc and one-line skill edits through one executor), not `fix.js`; the deterministic checks and the lib tests are the verification, no fix-verifier agents ran.
