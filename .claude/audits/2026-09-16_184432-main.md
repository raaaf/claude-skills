# Audit 2026-09-16 18:44 — main (eighth run, gate attempt after run-7 fixes)

## Result

1 Critical, 24 confirmed findings, 0 unverified. Every in-scope dimension `complete`
(`typography`, `ui_design`, `animation` skipped, no relevant files). Push marker NOT set by rule:
one Critical open. It was introduced by run 7's own hardening and is fixed in the follow-up commit
with a different design, not a patch.

## Scope

- Base HEAD `531f03c` (origin/main), head at start `bf59759`, 22 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`)
- find.js runId: `wf_d52feecf-19e` (48 agents, 0 errors, 455s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`,
  `FRESH_SHELL_RESULT=OK`

## Findings

### Fixed in the follow-up commit

- [Critical][security] design-audit/SKILL.md:204: run 7 replaced bare interpolation with `F='{datei}'`, which a path containing a quote breaks out of. Finding text now never enters a command line: the orchestrator writes `path<TAB>line` rows to `{AUDIT_TMP}/locations.tsv` with the Write tool and `audit/bin/validate-locations.sh` reads the file (`read -r`, everything quoted, rejects non-integer lines, absolute or `..` paths, missing files, out-of-range lines). Tested against quoted, absolute, traversal and non-numeric inputs.
- [Important][architecture] audit/SKILL.md:281: prose said the release runs in the Phase 4 block; that block is the conditional passed-marker touch and never called it. Release is its own unconditional sourced block.
- [Important][architecture] ship/SKILL.md:49 and :317: `TEST_COMMAND` set in Phase 0, consumed in Phase 2b, a fresh-shell violation for a variable that `check-fresh-shell.sh` (functions only) cannot see. Phase 2b re-derives it.
- [Important][code_quality] ship/SKILL.md:46: `deploy-command`/`health-check` still parsed with `grep | cut -d' '`, the pattern replaced for `test-command` one commit earlier. One parser, `orch_ship_value`, for all three.
- [Important][security] ship/SKILL.md:275: the content secret scan was gated by `&&` on resolving `pre-checks.sh`; an unresolved helper skipped the scan silently. It refuses to commit now.
- [Important][architecture] full-audit/SKILL.md:116: `orch_payments_guidelines` called in prose, not a sourced block. Fenced.
- [Important][code_quality] audit/workflows/find.js:171: agent type, prompt-file number and slug kept in three hand-maintained maps. One `DIMENSION_TABLE`; `ALL_DIMENSIONS` stays a literal because `run-evals.sh` scrapes that exact line, and an assertion keeps the two in step.
- [Important][docs_sync] audit/workflows/find.js:201: the mirrored `FRONTEND_EXT_RE` lacked `styl` after `lib-git-base.sh` gained it. Added.
- [Important][docs_sync] audit/bin/lib-git-base.sh:129: `AUDIT_DIMS` listed 12 dimensions and claimed `verify-agents.sh` reads it (it never did). 13 now, comment states the one real consumer and points at `DIMENSION_TABLE` as canonical.
- [Important][docs_sync] CLAUDE.md:109: `.claude/ship.md` missing from the project-overrides list. Listed with its three keys.
- [Important][architecture] agents/design-*.md: `maxTurns` lived only in the worker spec's bullet list, never in the frontmatter that is actually read. Added to both definitions.
- [Important][security] design-audit/agents/*.md: neither output contract had a field to surface an injection attempt. `INJECTION_NOTE:` line added to both.
- [Minor][docs_sync] CLAUDE.md:69: the frontmatter convention said `effort: high|xhigh` with no carve-out for `/ship` and `/delegate` at `medium`. Stated.
- [Minor][docs_sync] docs/references/subagents.md:135: still recommended the retired `"Keine Findings."` sentinel 67 lines after declaring it dead. Rewritten.
- [Minor][copy] audit/SKILL.md:43, ship/SKILL.md:174, delegate/SKILL.md:45: the lib-not-found messages in three wordings. One wording each for the fatal and the fail-open case, and the fatal one names the fix.

### Logged, not fixed

- [Important][security] audit/bin/lib-orchestrator.sh:50: marker paths predictable from `md5(cwd)`. Sixth run naming it.
- [Important][security] ship/SKILL.md:47: `HEALTH_URL` from repo config, curled without allowlist. Sixth run. Same class as `deploy-command:`, which does get an `AskUserQuestion`; worth the same gate on the next `/ship` pass.
- [Minor][performance] audit/workflows/find.js:350 and :707: `computeFloorFiles` twice per dimension, O(n²) dedupe. Fifth run.

## Open Points

- Whether one `UNCERTAIN` verdict should block the marker (runs 5 and 7). Design decision, still open.
- `check-fresh-shell.sh` covers functions, not variables crossing blocks (the `TEST_COMMAND` case above was found by a specialist). A variable-flow check would need block-level data-flow; not built.
