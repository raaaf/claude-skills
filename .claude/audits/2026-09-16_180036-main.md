# Audit 2026-09-16 18:00 — main (fifth run, orchestrator refactor)

## Result

1 Critical, 20 confirmed findings, 1 unverified. Push marker NOT set by rule: a Critical was open
and `code_quality` returned `incomplete` because one verdict stayed `UNCERTAIN` (not a coverage
gap: zero per-chunk coverage failures, zero cluster rejections in the diagnostics). Both fixed in
the follow-up commit; the next run decides the gate.

## Scope

- Base HEAD `531f03c` (origin/main), head at start `e8f2279`, 16 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`); `typography`, `animation`,
  `privacy` skipped, no relevant files
- find.js runId: `wf_06b4241f-dd7` (54 agents, 0 errors, 512s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `WORKFLOW_DUPES_RESULT=OK`
- `DIFF_CLASS=code`, eval validator 0 errors

## Findings

### Fixed in the follow-up commit

- [Critical][security] ship/SKILL.md:266: the pre-commit sensitive-file check grepped staged FILENAMES only, never content, and Phase 2 accepts an audit marker up to 1800s old, so a secret pasted into an innocently named file after the audit ran would reach the remote unscanned. `/ship` now also runs `pre-checks.sh` (content scan, same patterns as `/audit`, prints `file:line: pattern-name` only) before committing and stops on `FINDINGS`.
- [Minor][code_quality] audit/SKILL.md:107: `AUDIT_DIMENSIONS="$AUDIT_DIMENSIONS,payments"` prepends a comma on an empty selection. UNCERTAIN in verification (Phase 1.5 never yields an empty selection), guarded anyway with `${AUDIT_DIMENSIONS:+$AUDIT_DIMENSIONS,}payments`.
- [Important][architecture] design-audit/SKILL.md:117: Phase 1's zero-frontend early exit did not release the marker Phase 0 now claims, and its comment still described the old per-dispatch claim. Released on that exit too.
- [Important][architecture] design-audit/SKILL.md:25: the architecture note said the skill has NO agents of its own, false since it gained two today. Rewritten.
- [Important][docs_sync] full-audit/SKILL.md:82: `.claude/stripe-golive.md` was a project override file nowhere listed in CLAUDE.md's "Project-specific overrides". Listed.
- [Important][docs_sync] CLAUDE.md:31: the agentType summary still read as if `payments` fell under "the rest". Now names every dimension per agent.
- [Important][docs_sync] audit/SKILL.md:178: cited `find.js:726` for a guard that now sits at line 765. Cites the guard by its message instead of a line.
- [Minor][docs_sync] docs/references/subagents.md:68: still asserted "Keine Findings." is a live contract, contradicting CLAUDE.md's removal. Updated.
- [Minor][docs_sync] plan-it/SKILL.md:63 and CLAUDE.md:98: bash fallback `xhigh` and the table said `xhigh` while the frontmatter, which is what `${CLAUDE_EFFORT}` receives, says `high`. Both now `high`. The same reasoning corrected the CLAUDE.md claim that `/audit` and `/full-audit` default to `medium`: their frontmatter pins `high`, so `high` is the effective default and the `:-medium` fallback only matters if that line is removed.
- [Minor][docs_sync] audit/bin/lib-orchestrator.sh:11: the header presented one loop as what "every SKILL.md" uses; audit uses its own bin path and full-audit adds a candidate. Header says so.
- [Minor][copy] full-audit/SKILL.md:37: three fatal messages in one block, two English and one German. All German now (runtime user-facing strings may be German; mixing within one block was the defect).

### Logged, not fixed

- [Important][architecture] full-audit/SKILL.md:32: the lib-resolution loop carries a `CLAUDE_PROJECT_DIR`-derived candidate no other skill has. Deliberate: full-audit had that candidate before the refactor and the lib header now names it as the one documented deviation.
- [Important][architecture] audit/workflows/find.js:83: the "Duplicated in fix.js" comment convention is not applied to all four guarded symbols. Cosmetic; the guard is `check-workflow-dupes.sh`, not the comment.
- [Important][architecture] ship/SKILL.md:303 and audit/SKILL.md:216: three orchestrators run a repo-supplied test command. Documented in the CLAUDE.md gotcha since the fourth run.
- [Important][security] audit/bin/lib-orchestrator.sh:40: marker path predictable from `md5(cwd)`, no ownership check. Third run naming it; single-user `/tmp` convention.
- [Important][security] ship/SKILL.md:47: `HEALTH_URL` from the repo's own config, curled unfiltered. Fourth run naming it; same trust-boundary class as `test-command:`.
- [Minor][code_quality] audit/workflows/find.js:306: `chunkByDirectory`'s trailing merge cannot repair `chunks[0]`. Real, small; a too-small first chunk costs one extra specialist, nothing else.
- [Minor][code_quality] audit/SKILL.md:162 and full-audit/SKILL.md:136: the payments-floor jq merge is pasted in both. Candidate for the lib (`orch_payments_floor`) on the next pass; left out of today's refactor to keep it to the prologue.
- [Minor][performance] audit/workflows/find.js:705 and :350: O(n²) dedupe, `computeFloorFiles` twice per dimension. Fourth and third run naming them; harmless at current sizes.

## Open Points

- `code_quality: incomplete` came from an `UNCERTAIN` verdict, not from coverage. That is the contract (`find.js:563`): an unverified finding makes the dimension incomplete so it cannot vanish silently. It also means one UNCERTAIN Minor blocks the push marker. Whether that is the intended weight is a design question worth a decision, not a silent change here.
