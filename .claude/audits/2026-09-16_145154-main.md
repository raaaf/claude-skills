# Audit 2026-09-16 14:51 — main (third run, coverage hypothesis)

## Result

1 Critical, 32 confirmed findings. Push marker NOT set by rule: `docs_sync` returned `incomplete`.
The coverage hypothesis held for the specialists: `security` and `architecture`, `incomplete` in
both 2026-09-15 runs, came back `complete` after the per-chunk coverage contract landed in
`find.js`, and the new per-chunk diagnostics logged zero specialist coverage failures. The one
remaining `incomplete` is a scout-side validation rule, fixed below.

## Scope

- Base HEAD `3475378`, head at start `4265a58`, 19 changed files
- All 13 dimensions, `payments` not applicable (`STRIPE=no`); `ui_design` and `copy` `skipped, no
  relevant files` (does not block since `311f9db`)
- find.js runId: `wf_47c21bc1-60c` (50 agents, 0 errors, 572s)

## Mechanical checks (Phase 1)

- `SECRET_SCAN_RESULT=CLEAN`, `LOCKFILE_DRIFT_RESULT=CLEAN`, `BINARY_ARTIFACTS_RESULT=CLEAN`
- `SILENCING_RESULT=OK`, `DOCSCLAIM_RESULT=OK`, `AGENTS_RESULT=OK`, `DIFF_CLASS=code`
- Eval validator: 0 errors

## Findings

### Fixed in the follow-up commit

- [Critical][code_quality] audit/SKILL.md:107: Phase 1.5 appended `payments` to `SELECTED_DIMENSIONS`, a variable nothing initialised and Phase 2 never read; the `find.js` dispatch split `AUDIT_DIMENSIONS`, so the conditional dimension could never reach the pipeline. Same text inherited by `full-audit/SKILL.md`. One variable now, `AUDIT_DIMENSIONS`, end to end.
- [Important][code_quality] audit/SKILL.md:196: `TEST_COMMAND` used for the fix-wave baseline and passed to `fix.js`, derived nowhere. Phase 3 now derives it (`.claude/ship.md` `test-command:`, then manifest, then none) and `fix.js` no longer renders `test-lock.sh undefined` when it is empty.
- [Minor][code_quality] audit/SKILL.md:73: `AUDIT_BASE_HEAD` computed, never referenced. Removed.
- [Minor][code_quality] audit/evals/run-evals.sh:505: keyword loop that read and discarded every keyword after the hyphen check became obsolete. Removed with its dead counters.
- [Important][docs_sync] .claude/audits/2026-09-15_172500-main.md:5: header said 13 confirmed while the list held 17 lines. Header now states both numbers and why they differ.
- [Important][docs_sync] .claude/audits/2026-09-15_172500-main.md:54: "30/30 back-filled" against 29 keys in `patterns.json`; near-duplicate folding merged two. Wording corrected.
- [Important][docs_sync] CLAUDE.md:48: `check-skips.sh` row still described the triage routing that the 2026-09-05 rebuild removed. Row now says the script is unwired.
- [Important][docs_sync] CLAUDE.md:31: "collapsed-worker routing is gone" while `w3-frontend.md` exists and `/design-audit` dispatches it; the agentType summary also omitted `payments` (security-auditor). Both stated.
- [Important][docs_sync] CLAUDE.md:122: `set -f` located in "the Phase 1 block"; `scope.md` is `/full-audit`'s Phase 0. Corrected.
- [Minor][docs_sync] CLAUDE.md:102: "Phase 0.2" names a sub-phase `audit/SKILL.md` does not have. Now "Phase 0, second check".
- [Minor][docs_sync] CLAUDE.md:88: Effort table omitted `/design-audit`, which maps `CLAUDE_EFFORT` to `MAX_ELEVATION`/`VERIFY_FIXES`, and never stated `/audit`'s `medium` default. Both added.
- [Minor][docs_sync] design-audit/SKILL.md:302: prohibited an `AUDIT_STATUS:` line that no longer exists anywhere. Now names the live terminal-line contract.
- [Minor][docs_sync] plan-it/SKILL.md:225: an `Evidence:` citation outside Phase 1 with no pointer to the provenance note. Pointer added.
- [Minor][security] ship/SKILL.md:266: sensitive-filename grep without `-i`, so `SECRET.txt` passed. `-iE` now.
- Not a finding, the cause of `docs_sync: incomplete`: `find.js:434` rejected any cluster containing a file with `count: 0`. The scout had put `security-2026.md` (count 0) into a cluster about the split, which is exactly the file where the pattern is absent. A cluster now needs two files with a positive count and may carry zero-count files; rejections are logged with the reason.

### Discarded

- [Important][security] audit/SKILL.md:86: `AUDIT_DIMENSIONS`/`AUDIT_FIX_SCOPE` skip the Phase 1.5 consent gate. Documented tradeoff at the point of definition (a headless run must never hang on a question); the variables are set by the operator's own harness. Same verdict as run 1.
- [Important][security] audit/evals/run-evals.sh:1124: nested eval session with the OS sandbox disabled. Documented tradeoff in CLAUDE.md (a sandbox cannot start inside a sandbox; fixtures run in a throwaway repo). Same verdict as run 2.

### Logged, not fixed

- [Important][security] CLAUDE.md:28: the "subagents cannot write under `.claude/`" boundary is asserted five times and rests on incident reports, not a test. Worth a deterministic probe; separate work.
- [Important][security] audit/SKILL.md:76: the push marker path is derived from `md5(pwd)`, no ownership check before `[ -f ]`. Local `/tmp` convention on a single-user machine; noted, not changed.
- [Important][security] design-audit/SKILL.md:140: mapping and reference-verdict agents dispatched as bare `general-purpose` with inline briefings, no registered definition. Separate work.
- [Minor][security] ship/SKILL.md:78: `HEALTH_URL` derived from repo-committed config and curled. Same trust-boundary class as the `test-command:`/`deploy-command:` gotcha; log.
- [Important][architecture] delegate/SKILL.md:41, ship/SKILL.md:168, full-audit/SKILL.md:31, design-audit/SKILL.md:39, audit/SKILL.md:41, audit/SKILL.md:53: the orchestrator prologue (helper resolution, `AUDIT_ROOT` candidates, `verify-agents` guard, Stripe parse block, in-progress key) is pasted with variations across five skills instead of sourced once. Nine findings in this run describe that one condition. Deferred as one refactor (a shared `lib-orchestrator.sh`), not nine patches: each patch would add a tenth variant.
- [Important][architecture] full-audit/SKILL.md:42: `detect-framework.sh` parsed in Phase 0 and again by `scope.md`, `PLATFORM` dropped without comment. Part of the same refactor.
- [Important][architecture] audit/evals/run-evals.sh:292: canonical dimension list scraped from `find.js` source text at runtime; and dim_pattern_for() is a second hand-maintained table over the same ids. A JSON the two read would end both. Separate work.
- [Important][architecture] plan-it/SKILL.md:347: `plan-learning-agent` reimplements the audit learning contract. Known, logged in run 2 as well.
- [Important][performance] full-audit/references/scope.md:103: six separate `find` walks over `SOURCE_DIRS`. One walk with a post-filter; part of the scope refactor.
- [Minor][performance] audit/evals/run-evals.sh:368 and :754: one `jq` process per field per entry. Logged in run 2 as well.
- [Minor][performance] audit/workflows/find.js:694: `dedupeFindings` is O(n²) via `findIndex`. Harmless at current sizes.

## Open Points

- `docs_sync: incomplete` via `cluster:security-guideline-split-point`. Cause found and fixed in
  `find.js` (zero-count file in a cluster), not yet exercised by a run.
- The architecture-duplication family (nine findings, one cause) is the largest open item in the
  repo and is deliberately not patched piecemeal.
